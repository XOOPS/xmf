build:
  environment:
    php: 8.2
  nodes:
    analysis:
      tests:
        override:
          - php-scrutinizer-run

filter:
  excluded_paths:
    - '_archive/*'
    - 'tests/*'
    - 'vendor/*'
    - 'docs/*'
  dependency_paths:
    - 'stubs/'

tools:
  php_analyzer: true

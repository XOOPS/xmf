build:
  nodes:
    analysis:
      tests:
        override:
          - php-scrutinizer-run

filter:
  excluded_paths:
    - '_archive/*'
    - 'tests/*'
    - 'vendor/*'
    - 'docs/*'
    - 'stubs/*'

tools:
  php_analyzer: true
  external_code_coverage:
    timeout: 600
